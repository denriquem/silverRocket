import React from "react";
import classes from "./Header.module.css";

const Header = () => {
	return (
		<header className={classes.theHeader}>Welcome To Silver Rocket</header>
	);
};

export default Header;
